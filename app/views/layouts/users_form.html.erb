<!DOCTYPE html>
<html>
	<head>
		<title>Learnruby</title>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
		<%= csrf_meta_tags %>
	</head>
<body>

	<div class="pin-button">
		
		<% if @signup %>
		<%= link_to "#{t('users.login')}", :login, class: "ui green inverted pull-right button" %>
		<% else %>
		<%= link_to "#{t('users.signup')}", :signup, class: "ui green inverted pull-right button" %>
		<% end %>
	</div>
	<img id="rainyday" src="<%= asset_path("night.jpg") %>" />
	
	<div class="wallpaper">
	<%= yield %>
	</div>

</body>

</html>
